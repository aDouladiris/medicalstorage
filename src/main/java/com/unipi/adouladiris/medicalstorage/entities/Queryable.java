package com.unipi.adouladiris.medicalstorage.entities;

public abstract class Queryable {
    // Every entity that we want to query at database extends this abstract class.
    // We have placed check class types as arguments in order to confirm the correct class
    // during compilation time.
}
